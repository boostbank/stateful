var Stateful = require('./../src/index');

describe("Stateful Tests", () => {
  // beforeEach(() => {
  //   Stateful.clear();
  // });

  it("Should set the state", () => {
    // Stateful.createStore();
    // Stateful.modify(store => {
    //   store.test = "test";
    // });
    // expect(Stateful.getState().test).toBe("test");
  });
});
